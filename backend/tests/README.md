# TESTS
Executa os testes de funcionamento do sistema, utilizando Pytest

## Rodar os testes
**Comando para executar os testes**
```powershell
python -m poetry run pytest tests/ -v
```

> Todos os comandos devem ser executados dentro da pasta `backend/`